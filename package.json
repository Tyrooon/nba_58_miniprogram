{
  "name": "nba-58-miniprogram",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && npm run build && mkdir -p dist/public && cp -r ../webapp/* dist/public/",
    "start": "node server/dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
